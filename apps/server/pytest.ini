[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = test_*.py
testpaths = todos/tests
addopts = -ra --cov=config --cov=todos --cov-report=term-missing
markers =
    unit: fast, isolated tests (no HTTP, minimal/no DB).
    integration: full request/response through DRF with a real test DB.
